<?php

namespace App\Http\Controllers\Api\Auth;

use App\Http\Controllers\Controller;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Learnwithfair\SocialAuth\Http\Controllers\SocialAuthController as BaseSocialAuthController;

/**
 * SocialAuthController
 *
 * This controller extends the package base controller. Override any method
 * here to customise behaviour for your application without losing the ability
 * to pull in future package updates.
 *
 * Available methods to override:
 *   - findOrProvisionUser(mixed $socialiteUser, string $provider): User
 *   - downloadAvatar(?string $url, ?string $existingPath): ?string
 *   - resolvePlanId(): ?int
 *   - generateUniqueUsername(string $name): string
 */
class SocialAuthController extends BaseSocialAuthController
{
    /**
     * Example: add extra fields when provisioning a new user.
     *
     * Uncomment and modify as needed.
     *
     * protected function findOrProvisionUser(mixed $socialiteUser, string $provider): \App\Models\User
     * {
     *     $user = parent::findOrProvisionUser($socialiteUser, $provider);
     *
     *     // $user->referral_code = \Illuminate\Support\Str::upper(\Illuminate\Support\Str::random(8));
     *     // $user->save();
     *
     *     return $user;
     * }
     */
}
